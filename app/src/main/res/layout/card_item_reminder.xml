<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/cvReminder"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:clickable="true"
    android:focusable="true"
    app:strokeWidth="0dp"
    app:contentPadding="16dp"
    app:cardUseCompatPadding="true"
    app:cardCornerRadius="12dp"
    app:cardElevation="0dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:animateLayoutChanges="true">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_centerVertical="true"
            android:layout_marginTop="4dp"
            android:layout_marginBottom="4dp"
            android:layout_toLeftOf="@+id/btnDel"
            android:orientation="vertical">

            <TextView
                android:id="@+id/tvItemReminderTitle"
                android:layout_width="match_parent"
                android:ellipsize="end"
                android:maxLines="2"
                android:layout_height="wrap_content"
                android:textColor="@color/black"
                android:text="Title"
                android:textSize="16dp"/>

            <TextView
                android:id="@+id/tvItemReminderDate"
                android:layout_width="match_parent"
                android:ellipsize="marquee"
                android:singleLine="true"
                android:layout_height="wrap_content"
                android:textSize="13dp"
                tools:text="Date"/>

            <TextView
                android:id="@+id/tvItemReminderTime"
                android:layout_width="match_parent"
                android:ellipsize="marquee"
                android:singleLine="true"
                android:layout_height="wrap_content"
                android:textSize="13dp"
                tools:text="Time"/>

            <TextView
                android:id="@+id/tvItemReminderRepeat"
                android:layout_width="match_parent"
                android:ellipsize="marquee"
                android:singleLine="true"
                android:layout_height="wrap_content"
                android:textSize="13dp"
                tools:text="Repeat"/>

        </LinearLayout>

        <ImageView
            android:layout_width="wrap_content"
            android:layout_alignParentRight="true"
            android:layout_height="wrap_content"
            android:layout_marginLeft="16dp"
            android:layout_marginRight="16dp"
            android:src="@drawable/ic_delete"
            app:tint="@color/blue"
            android:layout_centerVertical="true"
            android:clickable="true"
            android:onClick="setDialog"
            android:background="?selectableItemBackground"
            android:id="@+id/btnDel"/>

    </RelativeLayout>

</com.google.android.material.card.MaterialCardView>